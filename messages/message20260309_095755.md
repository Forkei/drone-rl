# Update — Nav training underway

## All four fixes applied

**1. Reward scaling**: dist_w=0.1 (was 1.0), time_w=0.01 (was 0.05)
   - Max negative per episode now ~-52 (480 steps × 0.1×0.5m avg + 0.01 time)
   - Goal bonus +10 is now meaningful relative to accumulated penalty

**2. Curriculum by success rate**: advances when success_rate >= 0.60 over last 100 episodes
   - Tracks per-episode success flag from `_computeInfo()` via Monitor
   - Resets success deque on each stage transition (clean slate)

**3. Conservative clip_range**: clip_range=0.1, clip_range_vf=0.1 (was 0.2)
   - ent_coef back to 0.01 (0.005 caused collapse in PID run)

**4. Logging**: NavCallback logs to TensorBoard every check_freq steps:
   - `nav/success_rate` — rolling 100-ep success rate
   - `nav/mean_final_dist` — mean final distance to target at episode end
   - `nav/curriculum_stage` — current stage index (0/1/2)

## Nav training status — RUNNING
- Job ID: b7rbbh2vf, 2M steps, 8 envs, 638 fps
- First eval @ 20k: reward=-0.65 ± 0.08 (baseline: random policy sits ~0.65m from target)
- success_rate=0.00 at 20k (expected, policy hasn't learned yet)
- Logs: `./logs_nav/`, models: `./models_nav/`

## One architecture note
`env_method("_set_target_range", r)` works because SB3 DummyVecEnv forwards attr lookups
through Monitor.__getattr__ → NavAviary. Tested in smoke test. Clean.

## Obs/reward summary for your records
```
Obs (1, 60):
  [0:3]   x, y, z
  [3:6]   roll, pitch, yaw
  [6:9]   vx, vy, vz
  [9:12]  wx, wy, wz
  [12:57] action history (15 × 3)
  [57:60] rel_target = target - pos  ← relative vector

Reward per step:  -0.1 * dist_to_target - 0.01
Goal bonus:       +10.0 (dist < 0.1m for 3 consecutive steps)
Episode bounds:   ±2.5m XY, 0.05-3.0m Z, tilt < 0.6 rad, 10s timeout
```

## What I'll do when nav converges
Visualize across random seeds/angles first (as you suggested) before adding DR.
Will look for: consistent overshooting, heading-dependent failures, altitude bias.
Then DR: mass ±15%, drag ±20% per-episode at reset; wind as random per-step impulse.
