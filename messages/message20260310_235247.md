# SAC_3 Vision Training — Complete
**Date:** 2026-03-10

## What happened

- SAC_2 died unexpectedly at ~114k steps (cause unknown, likely OOM or manual kill). No replay buffer had been saved yet.
- Restarted as **SAC_3** from scratch — same params: `img_wh=(48,32)`, SmallCNN, 2M steps, CUDA.
- SAC_3 ran to completion (~8h wall time, 69.5 fps).

## Results

| Metric | Value |
|---|---|
| Final mean dist | 0.84m |
| Final success rate | ~1.5% |
| Best eval reward | -0.187 (at ~1.4M steps) |

The policy learned to navigate *toward* the sphere but doesn't reliably close the final ~0.1m. Success rate plateaued around 1–2% for the entire second half of training.

## Saved artifacts (`models_vision_nav/`)
- `vis_nav_final.zip` — 2M step final model
- `best_model.zip` — best eval checkpoint (~500k steps, reward -0.19)
- `replay_buffer.pkl` — 882MB (saved at end, HARD REQUIREMENT met ✓)
- Checkpoints every 100k (100k → 2M), all present

## Bugs fixed this session
- `check_vision.py` had f-string format spec errors (lines ~219) — fixed
- `check_vision.py` mini-bench and obs-frame functions were missing `img_wh=(48,32)` — fixed
- `check_vision.py` LOG_DIR was hardcoded to `SAC_2` — updated to `SAC_3`

## Thoughts for next steps

1. **Benchmark best_model.zip** — run compare_kin_vision.py or a vision-specific benchmark to get a real read on performance across fixed cases. The 10-episode EvalCallback is too noisy to judge.

2. **The plateau is suspicious** — success rate barely moved from 500k→2M. Possible causes:
   - Reward shaping issue: potential-based reward may not give enough dense signal in the final 0.1m
   - CNN not learning robust enough features from 48×32 images
   - `target_range=1.0m` means sphere can be up to 1m away at reset — hard for a purely visual policy with no memory

3. **Consider for SAC_4:**
   - Increase eval episodes (n_eval_episodes=20+) for less noisy metrics
   - Try frame stacking (train_vision_nav_stacked.py exists) — temporal info may help
   - Or: curriculum starting with `target_range=0.3m` to get early successes, then expand
   - Alternatively, add a small KIN privilege signal during early training (teacher-student)
